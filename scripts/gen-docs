#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=scripts/_helpers.sh
source "$SCRIPT_DIR/_helpers.sh"

ensure_venv
pushd "$ROOT_DIR" >/dev/null
with_env in_venv mkdocs build "$@"
popd >/dev/null
